/*import unit = require('tests/tsunit')
import test = require('tests/test')
import common = require('../common')

import KernaussageModel = require('../kernaussagemodel')
import KernaussageViewModel = require('../kernaussageviewmodel')
import KernaussageController = require('../kernaussagecontroller')
import TestKernaussageCommunicator = require('tests/testkernaussagecommunicator')

var batch = common.Callbacks.batch;

class TestClass extends unit.TestClass {
    test(cxt, r) {
        var model = new KernaussageModel.Model();
        model.id = 5;
        var viewModel = new KernaussageViewModel.ViewModel();
        var communicator = new TestKernaussageCommunicator();
        var controller = new KernaussageController.Controller(model, viewModel, communicator);
        
        communicator.setTestKa()
        
        batch([
            r => {
                var communicator = n
            }
        ], r);
    }
}*/ 
